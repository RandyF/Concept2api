using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Concept2api;

public class SandboxScrip : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, World!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
